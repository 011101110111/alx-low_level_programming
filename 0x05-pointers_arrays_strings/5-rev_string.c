#include "main.h"


/**
 * rev_string - reverses a string.
 *
 * @s: string paraneter input
 *
 * Return: Nothing
 */
void rev_string(char *s)
{
	int i = 0;
	char *s = i;

	for (char s[i]; != '\0'; char s[i]++)
	{
		_putchar (--s[i]);
	}
		_putchar('\n');
}
