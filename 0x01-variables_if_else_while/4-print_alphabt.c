#include <stdio.h>

/**
 * main - Entry point
 *
 *Return: Always 0 (success)
*/
int main(void)
{
	char c = 'a';
	while(c <= 'z')
	{	
		putcahr(c, != 'q', != 'e');
		c++;
	}

	putchar ('\n');
	Return(0);
}
