#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 (success)
 *
*/
int main(void)
{
	int digit = 0;

	while("%i" digit <= 9)
        {
		putchar(digit);
		digit++;
	}
	putchar("\n");

	return (0);
}
